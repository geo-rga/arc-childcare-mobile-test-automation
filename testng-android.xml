<!DOCTYPE suite SYSTEM "https://testng.org/testng-1.0.dtd">
<suite name="Android Suite" parallel="false" thread-count="1">

    <!-- TODO: Update build path and udid -->
    <!-- TODO: Update fullReset to "true" if you want run each test from a fresh install-->
    <test name="Android Tests">
        <parameter name="platform" value="android"/>

        <!-- TODO QA: Add path to app file and the build number when testing locally. Add in the "resources" directory-->
        <parameter name="build" value="src/test/resources/apps/android/android_staging_2038.apk"/>
        <parameter name="buildNumber" value="2003"/>

        <!-- TODO QA: To get the Android device udid, run the following in the terminal: adb devices -->
        <parameter name="deviceName" value=""/>
        <parameter name="udid" value="33071FDH2007QH"/>
        <parameter name="fullReset" value="true"/>

        <!-- TODO QA: Add correct environment when testing locally-->
        <parameter name="env" value="staging"/>
        <parameter name="isSimulator" value="false"/>



        <!-- TODO QA: Select the tests to be ran here -->
        <groups>
            <run>
                <include name="smoke"/>
            </run>
        </groups>
        <classes>
            <class name="tests.LoginTest"/>
            <class name="tests.WelcomeScreenTest"/>
            <class name="tests.CreateAccountTest"/>
            <class name="tests.ScheduleAppointmentTest"/>
        </classes>
    </test>
</suite>
