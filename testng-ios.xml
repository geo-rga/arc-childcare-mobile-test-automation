<!DOCTYPE suite SYSTEM "https://testng.org/testng-1.0.dtd">
<suite name="iOS Suite" parallel="false" thread-count="1">

    <!-- TODO: Update build path and udid -->
    <!-- TODO: Update fullReset to "true" to run each test from a fresh install-->
    <test name="iOS Tests">
        <parameter name="platform" value="ios"/>
        <parameter name="env" value="staging"/>
        <parameter name="build" value="src/test/resources/apps/ios/ios_blood_2244.ipa"/>

        <!-- TODO: To get the iOS device udid, run the following in the terminal: xcrun xctrace list devices-->
        <parameter name="udid" value="00008020-001354403E98002E"/>
        <parameter name="fullReset" value="true"/>

        <!-- TODO: Add test files to be ran here -->
        <classes>
            <class name="tests.LoginTest"/>
        </classes>
    </test>
</suite>
